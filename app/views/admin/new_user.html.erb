<h1>Admin Add User</h1>
<p>This page will add a user and assign them a random password. Data will not be validated. You will then be directed to a page where you can add the new user to a course.</p>
<%= form_for(:user, url: admin_user_path) do |f| %>
	<div class="top-label">
		<%= f.label :name %>
		<%= f.text_field :name, class: 'form-control' %>
	</div>
	<div class="top-label">
		<%= f.label :email %>
		<%= f.email_field :email, class: 'form-control' %>
	</div>
	<div class="top-label">
		<%= f.label :phone %>
		<%= f.text_field :phone, placeholder: '6042247245', class: 'form-control', maxlength: '10' %>
	</div>
	<div class="top-label">
		<%= f.label :age %>
		<%= f.number_field :age, class: 'form-control' %>
	</div>
	<div class="top-label">
		<%= f.label :health_notes %>
		<%= f.text_field :health_notes, class: 'form-control' %>
	</div>
	<%= f.label :waiver %>
	<%= f.check_box :waiver %>

	<%= f.hidden_field :password, value: @password  %>
	<%= f.hidden_field :password_confirmation, value: @password  %>
	<hr>
	<%= f.submit 'Create User', class: 'btn btn-primary' %>
<% end %>